import DashboardList from './DashboardList';
import PublicIcon from '@material-ui/icons/Public';

export default {
    name: 'dashboard',
    icon: PublicIcon,
    // list: DashboardList,
    list: DashboardList,
    options: {
        label: 'Inicio'
    }
}

